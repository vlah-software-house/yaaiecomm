# Server
PORT=8080
ADMIN_PORT=8081
BASE_URL=http://localhost:3000
ADMIN_URL=http://localhost:8081

# Database
DATABASE_URL=postgres://forge:forgedev@localhost:5432/forgecommerce?sslmode=disable

# Secrets
JWT_SECRET=dev-jwt-secret-change-in-production
SESSION_SECRET=dev-session-secret-change-in-production
TOTP_ISSUER=ForgeCommerce

# Stripe
STRIPE_SECRET_KEY=sk_test_fake
STRIPE_WEBHOOK_SECRET=whsec_fake
STRIPE_PUBLIC_KEY=pk_test_fake

# Media
MEDIA_STORAGE=local
MEDIA_PATH=./media

# SMTP (mailpit)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=store@forgecommerce.local

# VAT
VAT_SYNC_ENABLED=true
VAT_SYNC_CRON=0 0 * * *
VAT_TEDB_TIMEOUT=30s
VAT_EUVATRATES_FALLBACK_URL=https://euvatrates.com/rates.json
VIES_TIMEOUT=10s
VIES_CACHE_TTL=24h
